<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container header">
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="active"><a href="{{ path('platypus_homepage') }}">Home</a></li>
                <li><a href="{{ path ('platypus_userlist') }}">Users</a></li>
                <li><a href="{{ path ('platypus_postlist') }}">Posts</a></li>
            </ul>
        </div><!--/.nav-collapse -->
        <!-- user nav -->

        {% if app.user %}
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="glyphicon glyphicon-user"></span>&nbsp;
                        <strong>{{ app.user.firstName }}</strong>
                        <span class="glyphicon glyphicon-chevron-down"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <div class="navbar-login">
                                <div class="row">
                                    <div class="col-lg-4">
                                        <p class="text-center">
                                            <span class="glyphicon glyphicon-user icon-size"></span>
                                        </p>
                                    </div>
                                    <div class="col-lg-8">
                                        <p class="text-left"><strong>{{app.user.firstName }} {{  app.user.lastName }}</strong></p>
                                        <p class="text-left small">{{ app.user.email }}</p>
                                        <p class="text-left">
                                            <a href="{{ path('platypus_useredit') }}" 
                                               class="btn btn-primary btn-block btn-sm">Edit account</a>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </li>
                        <li class="divider"></li>
                        <li>
                            <div class="navbar-login navbar-login-session">
                                <div class="row">
                                    <div class="col-lg-12">
                                        <p>
                                            <a class="btn btn-danger btn-block" 
                                               href="{{ path('platypus_logout') }}">Close session</a>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </li>
                    </ul>
                </li>   
            </ul>
            <!-- user nav -->
        {% else %}
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('platypus_login') }}">Login</a></li>
                /
                <li><a href="{{ path('platypus_register') }}">Register</a></li>
            </ul>
        {% endif %}
    </div>
</nav>
<div class="container">
